import React, { useState } from 'react';

const App: React.FC = () => {
    const [count, setCount] = useState(0);

    return (
        <>
            <div className='text-6xl'>Hi</div>
        </>
    );
};

export default App;
